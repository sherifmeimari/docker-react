language: generic
sudo: required
services:
    - docker

before_install: 
    - docker build -t sherifmeimari/docker-react -f Dockerfile.dev .

script:
    - docker run sherifmeimari/docker-react npm run test -- --coverage